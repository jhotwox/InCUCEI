# FROM THE TUTORIAL
action_endpoint:
  actions_module: "actions"
  # Copilot
  # url: "http://192.168.100.32:3000/api/actions"

tracker_store:
   type: mongod
   url: mongodb+srv://incucei.ldif0fo.mongodb.net/
   db: chatbot
   username: chatbot
   password: passwordChatbot

# Allow rephrasing of responses using a Rasa-hosted model
nlg:
  type: rephrase
  llm:
    model_group: rasa_command_generation_model

model_groups:
  - id: rasa_command_generation_model
    models:
      - provider: rasa
        model: rasa/command-generator-llama-3.1-8b-instruct
        api_base: "https://tutorial-llm.rasa.ai"
